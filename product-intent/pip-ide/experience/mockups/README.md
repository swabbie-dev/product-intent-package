# Mockups

The current mockup is a proposed low-fidelity shell. The Figma file now contains
an agent-authored workspace concept at node `4:17`. The Design Lead has not
approved it. Each mockup must link to screens, flows, and components through
stable IDs.
