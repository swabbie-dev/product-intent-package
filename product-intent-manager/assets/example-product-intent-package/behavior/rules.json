{
  "rules": [
    {
      "id": "RULE-001",
      "scope_ids": [
        "CAP-001"
      ],
      "priority": 100,
      "when": [
        "one valid increment request is accepted"
      ],
      "then": [
        "increase persisted counter value by exactly one",
        "return the new value"
      ],
      "exceptions": [
        "failed requests do not change the value"
      ],
      "examples": [
        {
          "before": 4,
          "result": 5
        }
      ]
    }
  ]
}
