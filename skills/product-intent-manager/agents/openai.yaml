interface:
  display_name: "Product Intent Manager"
  short_description: "Create and maintain proportional Product Intent Packages"
  default_prompt: "Use $product-intent-manager to create or update a proportional Product Intent Package without unnecessary artifacts."
policy:
  allow_implicit_invocation: true
