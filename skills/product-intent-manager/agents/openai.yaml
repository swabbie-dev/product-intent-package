interface:
  display_name: "Product Intent Manager"
  short_description: "Create and maintain clear product intent artifacts"
  default_prompt: "Use $product-intent-manager to create or update a clear Product Intent Package from a new idea or existing product."
policy:
  allow_implicit_invocation: true
