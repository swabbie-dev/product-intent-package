interface:
  display_name: "Product Intent Manager"
  short_description: "Create and maintain proportional Product Intent Packages"
  default_prompt: "Use $product-intent-manager to create or update a proportional Product Intent Package, or preserve its current intent while implementing or auditing against it."
policy:
  allow_implicit_invocation: true
