# Physical runtime stack

```mermaid
flowchart LR
  %% Show each physical client, deployed service, managed platform, worker, data store, and external provider.
  %% Name the confirmed provider or runtime. If unknown, mark it proposed or blocked or link bounded discretion.
  %% State each node's responsibilities and owned state.
  %% Label each connection with its direction, protocol or transport, data or message meaning, and sync or async mode.
  %% Put security controls on the node, connection, or trust zone where they apply.
```
