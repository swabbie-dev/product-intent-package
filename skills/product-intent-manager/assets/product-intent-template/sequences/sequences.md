# Sequences

Use one `SEQ-*` section per consequential operation. Start with the linked
`FLOW-*` action or system event, use `ARCH-*` participants, and end at the
shared visible `SCREEN-*` outcome or `SM-*` state. Show ordered runtime work,
not screen navigation or a repeated condition matrix.

```mermaid
sequenceDiagram
  %% Add SEQ-* sections for multi-service, transactional, asynchronous, or failure-sensitive operations.
```
