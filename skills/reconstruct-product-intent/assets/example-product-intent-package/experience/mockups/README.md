# Mockups

Store or reference authority-confirmed mockups/prototypes here. Each file or external frame must have a `MOCK-*` entry in `governance/artifact-index.json` and links to `SCREEN-*`, `FLOW-*`, and `COMP-*` IDs.
